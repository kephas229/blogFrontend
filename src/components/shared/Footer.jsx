const Footer = () => (
    <footer className="border-top mt-auto py-4 text-center text-muted small">
        <span>© {new Date().getFullYear()} BlogFlow — Tous droits réservés</span>
    </footer>
);

export default Footer;
